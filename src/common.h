#ifndef _COMMON
#define _COMMON
#include <iostream>
#include <fstream>
#include <memory>
#include <string>
#include <algorithm>
#include <csignal>
#include <vector>

#define IS_INT(c) ((c>='0')&&(c<='9'))
#define SIZE_OF_CHAR_BUFFER 255
#define CHARTOINT(c) (c-'A')
#define INTTOCHAR(i) ((char)(i)+'A')
#define LENGTH_OF_ALPHABET 26

using namespace std;
#endif